{
  "id": "issue-12-live-herdr-559531f-v1",
  "repository": {
    "path": "/Users/kagami/dev/usine/.worktrees/issue-12l",
    "owner": "ariga39",
    "name": "usine"
  },
  "baseSha": "559531fef745ee08a41833b8e2692ede68ca9ffa",
  "instructions": "Write the exact frozen Task Contract JSON object from the coordinator prompt to repository-root task.json, pretty-printed with two spaces and a trailing newline. Then add only docs/FIRST_VERTICAL.md. Make that document non-empty and concise: state that this commit is the first live self-hosted Usine delivery, that project checks and fresh semantic review bind the same immutable candidate SHA, and that GitHub delivery uses the App without giving worker credentials. Do not modify implementation code, canonical documents, or any other path. Do not run git add or git commit; the host owns candidate finalization. Leave exactly task.json and docs/FIRST_VERTICAL.md as workspace changes.",
  "acceptance": [
    "The candidate diff from the exact base contains exactly task.json and docs/FIRST_VERTICAL.md.",
    "The committed task.json semantically matches the frozen Task Contract supplied by the coordinator.",
    "docs/FIRST_VERTICAL.md is non-empty and truthfully states the first live self-hosted Usine delivery, same-SHA project checks and fresh semantic review, and App-based GitHub delivery without worker credentials."
  ],
  "nonGoals": [
    "implementation changes",
    "canonical document changes",
    "monorepo structure changes",
    "additional candidate files",
    "automatic merge behavior"
  ],
  "projectCheck": {
    "command": "test \"$(git diff --name-only 559531fef745ee08a41833b8e2692ede68ca9ffa HEAD | sort | paste -sd, -)\" = \"docs/FIRST_VERTICAL.md,task.json\" && node -e 'const c=JSON.parse(require(\"node:fs\").readFileSync(\"task.json\",\"utf8\")); if(c.id!==\"issue-12-live-herdr-559531f-v1\"||c.baseSha!==\"559531fef745ee08a41833b8e2692ede68ca9ffa\"||c.delivery?.branch!==\"agent/12-live-characterization\") process.exit(1)' && grep -Fq \"first live self-hosted Usine delivery\" docs/FIRST_VERTICAL.md && grep -Fiq \"project checks and fresh semantic review bind the same immutable candidate SHA\" docs/FIRST_VERTICAL.md && grep -Fq \"GitHub delivery uses the App without giving worker credentials\" docs/FIRST_VERTICAL.md",
    "timeoutMs": 30000
  },
  "budget": {
    "maxImplementerActivations": 2,
    "maxReviewCycles": 2,
    "maxElapsedMs": 1200000
  },
  "authorization": {
    "source": "https://github.com/ariga39/usine/issues/12",
    "delivery": true
  },
  "delivery": {
    "baseBranch": "main",
    "branch": "agent/12-live-characterization",
    "issue": 12,
    "title": "Prove the first live reviewed delivery",
    "body": "Usine itself produced the exact-SHA reviewed PR.\n\nCloses #12"
  }
}
