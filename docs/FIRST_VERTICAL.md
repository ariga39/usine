# First live vertical

This commit is the first live self-hosted Usine delivery. Project checks and fresh semantic review bind the same immutable candidate SHA. GitHub delivery uses the App without giving worker credentials.
